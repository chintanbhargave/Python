Purpose is to get familiar with the pandas library.
The data is loaded into pandas data frame object from different file formats like .json,.xlsx,.csv,.txt.
Operations like Accessing,slicing,Adding,dropping,etc are performed on the data. 